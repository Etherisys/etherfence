# tests/fixtures/trust-home/bin/sample-tool's exact bytes are asserted
# against a hardcoded SHA-256 digest in
# crates/etherfence-setup/src/trust.rs. Without this, a Windows checkout
# with the default `core.autocrlf=true` would rewrite its LF line endings
# to CRLF, silently changing its hash and breaking that assertion.
tests/fixtures/trust-home/bin/sample-tool -text
